services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - 3000:3000
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-your_openai_api_key_here}
      OPENAI_MODEL: gpt-4o
<<<<<<< HEAD
      REDIS_URL: redis://redis:6379

  redis:
    image: redis:latest
    restart: unless-stopped
    ports:
      - 6379:6379
    volumes:
      - redis-data:/data

  redis-insight:
    image: redis/redisinsight:latest
    restart: unless-stopped
    depends_on:
      - redis
    ports:
      - 5540:5540
    volumes:
      - redis-insight-data:/data

volumes:
  redis-data:
  redis-insight-data:
=======

  recommender-db:
    image: pgvector/pgvector:pg18
    restart: unless-stopped
    environment:
      POSTGRES_DB: scrymagic_recommender
      POSTGRES_USER: recommender
      POSTGRES_PASSWORD: recommender
    ports:
      - 5432:5432
    volumes:
      - recommender_pgdata_pg18:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U recommender -d scrymagic_recommender"]
      interval: 10s
      timeout: 5s
      retries: 5

  recommender:
    build:
      context: ./sidecars/recommender
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-your_openai_api_key_here}
      DATABASE_URL: postgres://recommender:recommender@recommender-db:5432/scrymagic_recommender
      PORT: 3001
    ports:
      - 3001:3001
    depends_on:
      recommender-db:
        condition: service_healthy

  scheduler-db:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_DB: scrymagic_scheduler
      POSTGRES_USER: scheduler
      POSTGRES_PASSWORD: scheduler
    ports:
      - 5434:5432
    volumes:
      - scheduler_pgdata_pg18:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U scheduler -d scrymagic_scheduler"]
      interval: 10s
      timeout: 5s
      retries: 5

  scheduler:
    build:
      context: ./sidecars/scheduler
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: postgres://scheduler:scheduler@scheduler-db:5432/scrymagic_scheduler
      RECOMMENDER_API_BASE_URL: http://recommender:3001
      SCHEDULER_TICK_MS: 15000
      RECOMMENDER_POLL_MS: 5000
      RECOMMENDER_MAX_POLL_MS: 3600000
      PORT: 3002
    ports:
      - 3002:3002
    depends_on:
      scheduler-db:
        condition: service_healthy
      recommender:
        condition: service_started

volumes:
  recommender_pgdata_pg18:
  scheduler_pgdata_pg18:
>>>>>>> origin/feat/embeddings
